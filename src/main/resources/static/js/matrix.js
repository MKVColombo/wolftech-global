const canvas=document.getElementById('matrixCanvas');if(canvas){const ctx=canvas.getContext('2d');let pts=[];function resize(){canvas.width=innerWidth;canvas.height=canvas.clientHeight;pts=Array.from({length:55},()=>({x:Math.random()*innerWidth,y:Math.random()*canvas.height,vx:(Math.random()-.5)*.15,vy:(Math.random()-.5)*.15}))}function draw(){ctx.clearRect(0,0,canvas.width,canvas.height);pts.forEach((p,i)=>{p.x+=p.vx;p.y+=p.vy;ctx.fillStyle='rgba(148,163,184,.3)';ctx.beginPath();ctx.arc(p.x,p.y,1.4,0,7);ctx.fill();pts.slice(i+1).forEach(q=>{const d=Math.hypot(p.x-q.x,p.y-q.y);if(d<150){ctx.strokeStyle='rgba(56,189,248,'+(.06*(1-d/150))+')';ctx.beginPath();ctx.moveTo(p.x,p.y);ctx.lineTo(q.x,q.y);ctx.stroke()}})});requestAnimationFrame(draw)}resize();addEventListener('resize',resize);draw()}
